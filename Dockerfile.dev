FROM rust:1.96-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
  mold curl && rm -rf /var/lib/apt/lists/*

COPY --chmod=755 ./watch.sh /watch.sh

ENTRYPOINT [ "/watch.sh" ]

CMD [ "-x", "run" ]
